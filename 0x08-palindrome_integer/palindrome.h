#ifndef PALINDROME_H
#define PALINDROME_H

#include <stdio.h>
#include <stdlib.h>

int is_palindrome(unsigned long n);
unsigned long reverse(unsigned long n, unsigned long rev);

#endif /* PALINDROME_H */
